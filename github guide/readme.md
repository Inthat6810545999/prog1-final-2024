
1.เช็กสถานะ
git status
1.เลือกไฟล์ที่ต้องการจะpush
cd <project_folder>
2.เพิ่มไฟล์ที่ต้องการจะบันทึก
git add .
git add <file>
3.บันทึกการเปลี่ยนแปลงพร้อมข้อความอธิบาย
git commit -m "ข้อความอธิบายการเปลี่ยนแปลง"
4.ส่งการเปลี่ยนแปลงไปยังรีโมทรีโพซิทอรี
git push
git push origin <branch>
git push -u origin <branch>
5.ดึงการเปลี่ยนแปลงจากรีโมทรีโพซิทอรีมาไว้ที่เครื่องของเรา
git pull origin main


1) ดู remote ที่มีอยู่
git remote -v
2) เพิ่ม remote ใหม่ (เลือกreโปซิทอรีที่ต้องการ)
git remote add origin https://github.com/user/repo.git
3) เปลี่ยน URL ของ remote
git remote set-url origin <new_URL>
4) ลบ remote
git remote remove origin

git init
git clone <URL>


git branch 
ดูรายชื่อ branch ทั้งหมดใน repo

git branch <name>
สร้าง branch ใหม่ แต่ยังไม่สลับไป

git branch -d <branch>
ลบ branch แบบปลอดภัย (ต้อง merge แล้ว)

git branch -D <branch>
ลบ branch แบบบังคับ (ไม่สนว่าถูกรวมไหม)


git switch <branch>
สลับไปยัง branch ที่มีอยู่แล้ว

git switch -c <branch>
สร้าง และสลับไป branch ใหม่ทันที

git checkout <branch>
สลับไป branch

git checkout -b <branch>
สร้าง branch + checkout ไปพร้อมกัน

git merge <branch>
รวม commit ของ branch อื่นเข้ามาใน branch ที่คุณอยู่ตอนนี้

git rebase <branch>
นำ branch ปัจจุบันไปวางต่อท้าย branch เป้าหมาย
เพื่อให้ประวัติ commit เป็นเส้นตรง (clean)

🎯 สรุปแบบสั้นมาก
คำสั่ง	ใช้ทำอะไร
git branch	ดูรายชื่อ branch
git branch <name>	สร้าง branch ใหม่
git branch -d	ลบ branch แบบปลอดภัย
git branch -D	ลบ branch แบบบังคับ
git switch <branch>	สลับ branch
git switch -c <branch>	สร้าง + สลับ branch
git checkout <branch>	สลับ branch (แบบเก่า)
git checkout -b <branch>	สร้าง + สลับ (แบบเก่า)
git merge <branch>	รวมงานเข้ามา
git rebase <branch>	จัดประวัติ commit ให้เป็นเส้นตรง

